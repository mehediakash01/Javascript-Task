// Create different variables, each containing either an array or a non-array value.

// Now use isArray to check if each variable is an array.

// Print a message to the console indicating whether each variable is an array or not.

var arr = [1,2,3,4];
var Goat = 'ron';
var narr = 12345;
var goat = ['r','o','n'];


 Array.isArray(arr)?console.log('this an array'):console.log('not an array');
 Array.isArray(Goat)?console.log('this an array'):console.log('not an array');
 Array.isArray(narr)?console.log('this an array'):console.log('not an array');
 Array.isArray(goat)?console.log('this an array'):console.log('not an array');
 

